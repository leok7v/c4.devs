c4
# Build folder
build/
# Temporary test directory
tmp/
# Personal Claude Code overrides (committed: .claude/ itself)
.claude/settings.local.json
CLAUDE.local.md
# File-backed subagent I/O — per-session artifacts, not source.
# Anchored to the project root so they don't shadow lowercase
# task.md / response.md inside .claude/commands/ on case-insensitive
# filesystems (macOS).
/TASK.md
/RESPONSE.md
# Claude auto-memory (user-specific, machine-local)
.claude/projects/
# Debug scratch files
script.lldb
# Local borrowing materials
local/
